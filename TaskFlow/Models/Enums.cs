namespace TaskFlow.Models
{

    public enum TaskPriority
    {
        Low,
        Medium,
        High
    }

    public enum TaskStatus
    {
        InProgress,
        Completed
    }
namespace TaskFlow.Models;

public enum TaskPriority
{
    Low,
    Medium,
    High
}

public enum TaskStatus
{
    InProgress,
    Completed
}
